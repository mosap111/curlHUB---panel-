# 🧠 ملف ذاكرة المشروع وسجل الحالة (Project Memory & Status Log)
**مسار المشروع:** `/root/server_panel`  
**تاريخ التحديث:** 17 أغسطس 2026  
**النطاق المرتبط:** `http://code.yemensmm.me` (أو داخلياً عبر المنفذ `8090`)

---

## 📌 1. الملخص التنفيذي وما تم إنجازه (Work Completed)

### أ. فحص وتحصين السيرفر ضد هجمات التخمين (Server Hardening & Attack Neutralization):
1. **رصد الهجمات النشطة:**
   * تم رصد أكثر من **4,460+** محاولة تخمين لكلمات المرور عبر الـ SSH اليوم من روبوتات خارجية.
   * تم التأكد من استقرار السيرفر وأن استهلاك المعالج طبيعي جداً (`Load: 0.42`).
2. **الحماية على مستوى الكيرنل (Kernel-Level Drop):**
   * تحويل جدار الحماية إلى وضع `ufw limit 22/tcp` لمنع الاتصالات السريعة والمتكررة قبل وصولها إلى المعالج.
3. **تفعيل نظام حظر Fail2Ban التراكمي (5 سجون نشطة):**
   * `sshd`: حظر محاولات تخمين الدخول.
   * `recidive`: مضاعفة الحظر لمدة أسبوع إلى شهر للمهاجمين المكررين (`bantime.increment = true`).
   * `nginx-botsearch` & `nginx-bad-request`: حظر أدوات الفحص وماسحات الثغرات.
   * `nginx-limit-req`: حظر محاولات إغراق خادم الويب بالطلبات.
4. **حماية خادم Nginx:**
   * إضافة فلتر لحظر أدوات الفحص المكشوفة (*sqlmap, dirbuster, nikto, wpscan, masscan, nmap*).
   * تطبيق `limit_req_zone` لحماية المواقع من هجمات التعطيل والإغراق.

---

### ب. تطوير وتوسيع لوحة التحكم (Server Panel Full Supercharge):
تمت ترقية لوحة التحكم بإضافة 5 أقسام رئيسية ومتقدمة:

1. **🤖 مدير التطبيقات والبوتات (Bots & Python Apps Manager - `tab-bots`):**
   * رصد تلقائي لبوتات ومشاريع بايثون في السيرفر:
     * `YMS Telegram Bot` (`/root/yms_project/bot.py`)
     * `YMS Web App` (`/root/yms_project/app.py`)
     * `Shop Telegram Bot` (`/root/shop/botshop.py`)
   * التحكم الفوري بالبوتات (تشغيل / إيقاف / إعادة تشغيل).
   * عرض حي لاستهلاك المعالج والرام والـ PID ومدة العمل (Uptime).
   * قارئ سجلات الكونسول الحية والأخطاء (Live Console Logs Viewer).
   * إمكانية تسجيل أي بوت أو سكريبت جديد بسهولة عبر نافذة مخصصة.

2. **🛡️ تكامل سجون Fail2Ban وجدار الحماية في درع الأمان (`tab-security`):**
   * عرض تفاعلي لجميع سجون Fail2Ban في النظام وإحصائيات الحظر لكل سجن.
   * جدول بجميع عناوين IP المحظورة في النظام ككل مع زر فك حظر فوري (`Unban`).
   * أدوات الحظر اليدوي والتفريغ المباشر للسجلات.

3. **💾 مدير النسخ الاحتياطي السريع والاستعادة (`tab-backups`):**
   * أزرار سريعة بنقرة واحدة (1-Click Presets) لأخذ نسخ احتياطية مضغوطة `.tar.gz` لـ:
     * بوت المتجر (`Shop`)
     * مشروع `YMS Project`
     * جميع قواعد البيانات في السيرفر (`Databases .db`)
     * كود لوحة التحكم (`Server Panel`)
   * تنزيل ملفات النسخ الاحتياطي مباشرة إلى جهاز المستخدم عبر المتصفح.
   * حذف أو استعادة النسخ الاحتياطية بسهولة.

4. **🗄️ مستعرض واستوديو قواعد بيانات SQLite (`tab-db`):**
   * اكتشاف تلقائي لكافة ملفات قواعد البيانات (`.db`, `.sqlite`) في السيرفر.
   * عرض جداول وهيكل وأعمدة كل قاعدة مع عدد السجلات.
   * محرر استعلامات SQL تفاعلي لتنفيذ أوامر البحث والتعديل وعرض النتائج في جدول سريع مع توضيح وقت التنفيذ بالميلي ثانية.

5. **📜 مستعرض سجلات السيرفر الموحد (`tab-logs`):**
   * قارئ مركزي لسجلات: Nginx Access، Nginx Errors، Fail2Ban، SSH Auth، و Kernel/Dmesg.
   * شريط فلترة وبحث حي وتحديث تلقائي للسجلات.

6. **🔔 نظام إشعارات وتنبيهات تليجرام (`tab-settings`):**
   * إمكانية ربط Bot Token و Chat ID.
   * خيارات لتلقي إشعارات فورية عند: صد هجوم أمني، توقف أي بوت، أو ارتفاع الحمل.
   * زر فوري لتجربة إرسال تنبيه تجريبي للتأكد من الربط.

---

## ⏳ 2. ما كنا ننتظره وما هي الخطوات القادمة (What We Were Waiting For & Next Steps)

1. **الخطوة التالية المباشرة:**
   * بعد الانتهاء التام من كتابة الأكواد والتحقق من صحة القواعد والصياغات دون أخطاء، الخطوة المتبقية هي **إعادة تشغيل خدمة اللوحة (`systemctl restart server-panel` أو إعادة تشغيل uvicorn)** لتفعيل كافة الواجهات والميزات الجديدة أمام المستخدم.
2. **الخطوات المستقبلية المتاحة للتطوير الإضافي:**
   * **إضافة تنبيهات المراقبة الدورية (Cron Health Watchdog):** فحص صحة البوتات كل دقيقة وإعادة تشغيلها تلقائياً وإرسال رسالة تليجرام في حال توقف أي منها.
   * **مدير نطاقات Nginx وشهادات SSL المجانية (Certbot GUI):** إضافة شهادات SSL وتوليدها بضغطة زر لنطاقات جديدة من اللوحة مباشرة.
   * **النسخ الاحتياطي السحابي الخارجي:** رفع النسخ الاحتياطية تلقائياً إلى Telegram Channel أو Google Drive أو S3.

---

## 📊 3. البنية البرمجية المعتمدة (Architecture Reference)

```
/root/server_panel/
├── app.py                      # الخادم الخلفي (FastAPI) مع كافة الـ 49 Endpoints الجديدة
├── config.json                 # ملف الإعدادات وهاش الدخول وتوكن تليجرام
├── backups/                    # مجلد حفظ النسخ الاحتياطية المضغوطة
├── README.md                   # التوثيق الشامل والمفصل للمنصة
├── MEMORY.md                   # ملف الذاكرة الحالي لحفظ الحالة
└── static/
    ├── index.html              # واجهة المستخدم بجميع التبويبات والمودالات الجديدة
    ├── style.css               # التصميم المتطور بنمط Glassmorphism
    ├── app.js                  # المنطق البرمجي التفاعلي للعميل والربط مع الواجهات
    └── arabic-reshaper.js      # محرك معالجة ومحاذاة اللغة العربية للطرفية
```
